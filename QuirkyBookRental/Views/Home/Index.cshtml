@model IEnumerable<QuirkyBookRental.ViewModel.ThumbnailBoxViewModel>
@{
    /**/

    ViewBag.Title = "Home Page";
}
@using (Html.BeginForm("Index", "Home", FormMethod.Get))
{

    <br />
    @*<div class="row">
            <div class="pull-right" style="padding-right:15px;">
                <button type="submit" value="Search" class="btn btn-sm btn-primary">
                    <span class="glyphicon glyphicon-search"></span>Search
                </button>
            </div>

            <div class="pull-right">
                @Html.Editor("search", new { htmlAttributes = new { @class = "form-control input-sm" } })
            </div>

        </div>*@


    <div class="row">
        <div class="col-sm-8 pull-right">
            <div class="row">
                <div class="col-sm-14 text-right">
                    <label class="radio-inline">
                        @Html.RadioButton("option", "title") Title
                    </label>
                    <label class="radio-inline">
                        @Html.RadioButton("option", "author") Author
                    </label>
                    <label class="radio-inline">
                        @Html.RadioButton("option", "genre") Genre
                    </label>
                    <label class="radio-inline">
                        @Html.RadioButton("option", "year") Year
                    </label>
                </div>
            </div>
            <div class="row">
                <div class="pull-right">
                    <button type="submit" name="submit" value="Search" class="btn btn-sm btn-primary">
                        <span class="glyphicon glyphicon-search"></span> &nbsp; Search
                    </button>
                </div>
                <div class="pull-right">
                    @Html.Editor("search", new { htmlAttributes = new { @class = "form-control input-sm" } })
                </div>
            </div>
        </div>

    </div>

    <br />
}
<br />

<div class="row">
    @foreach (var area in Model)
    {
        @Html.Partial("_ThumbnailAreaPartial", area)
    }
</div>
